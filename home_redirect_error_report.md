# EduCode AI 首页跳转逻辑异常排查报告

## 故障现象
用户反馈：访问 `http://localhost:5173/` 时，页面直接进入了登录后的主界面（Dashboard），而不是预期的登录界面。

## 故障分析
通过审计 `App.tsx` 中的前端代码，我们发现了以下开发调试阶段遗留的代码逻辑：

```typescript
// App.tsx 约 144 行
// ---- 暂时跳过登录逻辑：如果未登录，自动使用测试账号 s001 登录 ----
if (!token) {
  try {
    const res = await login('s001', '123456');
    token = res.token;
  } catch (e) {
    console.error('自动登录失败', e);
  }
}
// -------------------------------------------------------------------
```

**原因分析：**
1. **硬编码自动登录**：在 `App` 组件初始化验证（`initAuth`）时，程序被设定为如果检测不到登录令牌（Token），则自动尝试使用账号 `s001` 和密码 `123456` 进行登录。
2. **调试遗留**：这通常是开发人员为了提高开发效率而添加的快捷登录逻辑，但在交付或测试环境会导致正常的登录流程被跳过。

## 已采取的措施
1. **删除冗余代码**：在 [App.tsx](file:///c:/Users/张广川/Desktop/educode-ai/educode-ai/App.tsx) 中彻底删除了上述自动登录的代码块。
2. **恢复正常验证流程**：现在的逻辑为：如果 `localStorage` 中没有有效的 `educode_token`，应用将保持 `user` 状态为 `null`，从而触发渲染 `LoginPage` 组件。

## 当前状态
- **首页访问路径**：访问 `http://localhost:5173/` 现在会正确检查登录状态。
- **登录界面**：未登录用户将被引导至登录页面。

## 验证建议
1. 请刷新浏览器页面。
2. 如果页面仍然直接进入主界面，请手动点击页面右上角的“退出登录”按钮，或清除浏览器缓存/LocalStorage。
3. 退出后再次刷新，应当看到蓝色的“智教编程 EduCode”登录界面。
