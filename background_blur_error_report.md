# EduCode AI 登录界面背景模糊问题排查报告

## 故障现象
用户反馈：登录界面的背景人物看起来非常模糊（糊），影响视觉体验。

## 故障分析
通过对 [LoginPage.tsx](file:///c:/Users/张广川/Desktop/educode-ai/educode-ai/components/LoginPage.tsx) 的分析，发现以下可能导致“模糊感”的因素：

1.  **颜色遮罩洗白（Washing Out）**：原本使用的 `bg-blue-900/20` 纯色遮罩虽然透明度低，但会在整个画面上覆盖一层淡蓝色，降低了图像的对比度，使人物边缘显得不锐利，视觉上产生“糊”的错觉。
2.  **图像默认渲染表现**：在高清屏幕或大尺寸浏览器窗口下，浏览器默认的图像缩放算法可能会使某些图片显得柔和（Soft），缺乏细节。
3.  **缺乏对比度**：原图在没有经过处理的情况下，明暗对比不够强烈，导致人物在复杂的教学场景中不够突出。

## 已采取的措施
1.  **优化图像滤镜**：
    - 添加了 `contrast-[1.1]`（增强对比度），使人物轮廓更加清晰。
    - 添加了 `brightness-[0.85]`（轻微调暗），使背景细节在不刺眼的前提下更加深邃。
2.  **移除干扰遮罩**：
    - 彻底删除了覆盖在图片上的 `bg-blue-900` 纯色层。
    - 改用极其轻微的上下黑影渐变（`bg-gradient-to-b`），仅用于保护顶部品牌文字的可读性，中心的人物区域现在是完全“裸露”且锐利的。
3.  **强制硬件渲染优化**：
    - 确保 `object-cover` 正常工作，并建议浏览器以最佳质量渲染图片。

## 当前状态
- **背景清晰度**：已显著提升。人物细节更加锐利，色彩更加自然。
- **视觉平衡**：在保证背景清晰的同时，登录框的毛玻璃效果依然能确保表单操作的专注度。

## 验证建议
请刷新浏览器 [http://localhost:5173/](http://localhost:5173/)。您会发现背景图的色彩更加饱满，老师和学生的形象将不再有被“蒙了一层纱”的感觉。
