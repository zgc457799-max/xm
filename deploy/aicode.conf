server {
    listen 80;
    server_name aicode.csicollege.cn;

    # 如果配置了SSL证书，请取消注释以下HTTPS强制跳转，并配置443 server块
    # return 301 https://$host$request_uri;

    # 1. 静态前端文件托管 (通过 Vite 打包出的 dist 目录)
    location / {
        root /var/www/aicode;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # 2. 代理后端 API 接口到 Docker 容器 (端口3001)
    location /api {
        proxy_pass http://127.0.0.1:3001/api;
        
        # 传递真实IP等头信息
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持 (如果代码判题有使用)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# --- HTTPS 配置示例 (准备好证书后取消注释并修改路径) ---
# server {
#     listen 443 ssl http2;
#     server_name aicode.csicollege.cn;
#
#     ssl_certificate /etc/nginx/ssl/aicode.csicollege.cn.pem;
#     ssl_certificate_key /etc/nginx/ssl/aicode.csicollege.cn.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         root /var/www/aicode;
#         index index.html index.htm;
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api {
#         proxy_pass http://127.0.0.1:3001/api;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
