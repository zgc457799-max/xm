# EduCode AI 登录故障排查报告

## 故障现象
用户尝试使用提供的账号（admin, t001, s001, s002）和密码（123456）登录系统时，系统提示“密码错误”。

## 故障分析
通过查看后端运行日志，我们发现了以下关键信息：
`[Login Debug] User: s001, Input Pwd: 123456, Hash in DB: $2a$10$2wGgNV3Wd/tMRUsBzkAtAe3kS5cQ79U.S0mSPPBB6.D5RI/Yi6A57y, Match: false`

**原因分析：**
1. **哈希不匹配**：数据库中存储的密码哈希值 `$2a$10$2wGgNV3Wd/tMRUsBzkAtAe3kS5cQ79U.S0mSPPBB6.D5RI/Yi6A57y` 与明文密码 `123456` 经过 `bcrypt` 算法加密后的结果不一致。
2. **初始化脚本问题**：在 `seed_data.sql` 脚本中提供的示例哈希值并非 `123456` 的正确哈希，导致预置数据无法直接通过密码验证。

## 已采取的措施
1. **更新修复脚本**：修改了 `server/src/scripts/fix_hashes.ts` 脚本，使其涵盖所有预置账号（admin, t001, s001, s002）。
2. **重置密码哈希**：运行了该脚本，利用 `bcrypt` 重新生成了 `123456` 的正确哈希并更新至数据库中。

## 当前状态
- **所有账号密码已重置为：`123456`**
- 验证状态：后端日志显示哈希已更新，现在可以正常匹配。

## 建议
请再次尝试登录。如果仍有问题，请检查：
1. 前端是否选择了正确的登录通道（学生/教师）。
2. 确保没有多余的空格输入。
