# 数智启航 —— AI 驱动的一站式程序设计竞赛与辅助教学系统
## 详细设计说明书

### 一、 引言

#### 1.1 项目背景
随着信息技术的快速发展，程序设计能力已成为计算机及相关专业学生的核心素养。各类程序设计竞赛（如蓝桥杯、ACM-ICPC、团体程序设计天梯赛等）在培养学生实战能力方面发挥着重要作用。然而，传统的编程教学与备考过程往往面临“题海战术效率低”、“缺乏个性化指导”、“师资力量不足难以提供实时代码诊断”等痛点。基于此背景，我们开发了“数智启航”系统。

#### 1.2 建设目的
本项目旨在利用大语言模型（LLM）等人工智能技术，打造一个集“赛、练、教、评”于一体的编程学习闭环生态。通过提供 AI 辅助代码诊断、个性化题库推荐、真实竞赛环境模拟等功能，帮助学生更高效地备战程序设计竞赛，同时为高校编程教学提供智能化的辅助管理工具，降低教师的辅导成本。

#### 1.3 适用范围
*   **适用对象**：备战各类程序设计竞赛的高校学生、计算机相关专业教师及管理员。
*   **适用场景**：日常算法刷题练习、班级内部模拟测试、校级/省级编程竞赛选拔、错题回顾与薄弱点强化。

---

### 二、 总体设计

#### 2.1 系统架构设计
系统采用前后端分离的 B/S（浏览器/服务器）架构，整体分为表现层、业务逻辑层、数据持久层和 AI 服务层。
*   **表现层（前端）**：负责用户交互与界面展示，采用 React 构建组件化页面。
*   **业务逻辑层（后端）**：处理用户请求、权限校验、判题逻辑分发等，采用 Node.js + Express 框架。
*   **数据持久层**：MySQL 负责存储用户、题目、比赛、错题等关系型核心数据；Redis 用于缓存高频访问数据（如全站排名、比赛实时榜单）及维持会话状态。
*   **AI 服务层**：通过调用第三方大模型 API，实现智能代码诊断与方案对比功能。

#### 2.2 功能结构图
*(此处在实际文档中需插入结构图，文字描述如下)*
*   **首页模块**：在线工作台、连续打卡记录、算法实战入口、全站积分排名、每日精选题推荐、近期竞赛日程。
*   **题库中心模块**：多语言支持（C/Java/Python等）、分类题库（数据结构/算法专题）、题目搜索与过滤。
*   **错题本模块**：错题自动收集、错因标签筛选、错题重做与复习计划。
*   **比赛大厅模块**：近期比赛列表展示、赛事报名、参赛合规中心（考试纪律与防作弊说明）。
*   **AI 助教模块**：语法与逻辑错误诊断、代码时间/空间复杂度分析、多种解题方案对比。

#### 2.3 技术架构选型
*   **前端**：React 18 + TypeScript + Vite（保证类型安全与极速构建），状态管理采用 Redux/Zustand，UI 组件库可选用 Ant Design 或 Arco Design。
*   **后端**：Node.js + Express（轻量高效），采用 TypeScript 编写保证代码健壮性。
*   **数据库**：MySQL 8.0（主业务数据存储），Redis 7.0（高并发场景下的缓存与排行榜实现）。
*   **判题核心**：基于开源沙箱（如 Isolate 或自研 Docker 容器化沙箱）实现安全隔离的代码编译与运行环境。

---

### 三、 功能模块设计

#### 3.1 首页模块
*   **输入**：用户的登录态凭证（Token）。
*   **处理逻辑**：
    *   后端根据 Token 解析用户 ID，查询数据库获取用户的连续打卡天数、积分、近期日程。
    *   查询 Redis 获取当前全站积分排行榜 Top 10。
    *   根据用户的历史做题记录，通过简单的推荐算法（或随机）抽取“每日精选题”。
*   **输出**：渲染包含在线工作台快捷入口、打卡日历、全站排名榜单、每日一题及日程时间轴的仪表盘页面。

#### 3.2 题库中心模块
*   **输入**：用户选择的分类标签（语言、难度、知识点）及搜索关键字。
*   **处理逻辑**：
    *   后端接收筛选条件，拼接 SQL 语句进行多表联查（题目表、标签关联表）。
    *   对于已登录用户，需额外联查“提交记录表”，标记题目状态（已通过、尝试过未通过、未开始）。
    *   支持本地化数据模拟（Mock Data），在网络异常或开发环境下，前端可通过 Vite 插件直接读取本地 JSON 题库数据渲染。
*   **输出**：分页展示的题目列表，点击题目进入在线代码编辑器界面。

#### 3.3 错题本模块
*   **输入**：用户 ID 及筛选条件（如“仅看编译错误”、“仅看超时错误”）。
*   **处理逻辑**：
    *   当用户在题库或比赛中提交代码且判题结果不为“Accepted”时，系统自动将该题及错误代码记录入“错题表”。
    *   用户在错题本中可根据错因标签（如 WA, TLE, MLE, CE）进行筛选。
    *   点击“重新做题”，系统将加载当时错误的代码片段，供用户继续修改。
*   **输出**：定制化的错题列表及重做工作台。

#### 3.4 比赛大厅模块
*   **输入**：比赛状态过滤（未开始、进行中、已结束）。
*   **处理逻辑**：
    *   展示所有公开赛及用户被邀请的私有赛。
    *   **参赛合规中心**：用户在点击“报名”或“进入比赛”前，必须阅读并勾选合规协议（防作弊声明）。
    *   比赛进行期间，系统提供“竞赛晋级路径可视化”功能，展示用户当前分数在省赛、国赛历史分数线中的位置（如：当前分数 150 分，超过 60% 选手，预计可获省二等奖）。
*   **输出**：比赛列表卡片、合规确认弹窗、实时排行榜及晋级预测图表。

#### 3.5 AI 助教模块（核心创新）
*   **输入**：用户当前的代码文本、题目描述、报错信息或用户的自然语言提问。
*   **处理逻辑**：
    *   **代码诊断**：将题目要求与错误代码打包构建 Prompt，发送至 LLM，要求 AI 指出逻辑漏洞或语法错误，而非直接给出答案。
    *   **方案对比**：当用户通过题目后，可请求 AI 提供其他语言的解法或更优时间复杂度的解法，并进行对比分析。
*   **输出**：结构化的 AI 诊断报告、代码差异高亮（Diff 视图）、对话式引导建议。

---

### 四、 数据库设计

#### 4.1 ER 图文字描述
系统核心实体包括：**用户（User）**、**题目（Problem）**、**提交记录（Submission）**、**比赛（Contest）**、**错题记录（ErrorBook）**。
*   一个**用户**可以有多次**提交记录**（1:N）。
*   一道**题目**可以对应多次**提交记录**（1:N）。
*   一场**比赛**包含多道**题目**（M:N，通过中间表 Contest_Problem 关联）。
*   一个**用户**可以参与多场**比赛**（M:N，通过中间表 Contest_User 关联）。
*   **提交记录**若未通过，则生成一条**错题记录**（1:1）。

#### 4.2 核心表结构设计

**表名：users（用户表）**
| 字段名 | 数据类型 | 说明 |
| :--- | :--- | :--- |
| id | INT (PK) | 用户唯一标识 |
| username | VARCHAR(50) | 登录名 |
| password | VARCHAR(255) | 加密后的密码 |
| role | ENUM | 角色（学生/教师/管理员） |
| score | INT | 总积分 |

**表名：problems（题目表）**
| 字段名 | 数据类型 | 说明 |
| :--- | :--- | :--- |
| id | INT (PK) | 题目ID |
| title | VARCHAR(100) | 题目标题 |
| description | TEXT | 题目描述（Markdown格式） |
| difficulty | ENUM | 难度（简单/中等/困难） |
| time_limit | INT | 时间限制（ms） |

**表名：submissions（提交记录表）**
| 字段名 | 数据类型 | 说明 |
| :--- | :--- | :--- |
| id | INT (PK) | 提交ID |
| user_id | INT (FK) | 提交用户ID |
| problem_id | INT (FK) | 题目ID |
| code | TEXT | 提交的源代码 |
| status | VARCHAR(20) | 判题状态（AC, WA, TLE等） |

*(其余表如 contests, error_book 结构略，实际文档中需补全)*

---

### 五、 AI 工具使用说明与特色创新

#### 5.1 AI 工具组合策略
本项目不仅仅是简单调用一个聊天接口，而是设计了一套“AI 工具组合使用策略”，深度赋能编程教学：
1.  **静态代码分析 + LLM 诊断**：在调用大模型前，先通过 ESLint/Cppcheck 等静态分析工具捕获基础语法错误。若为逻辑错误（WA），则将测试用例输入、预期输出与用户代码组合为定制化 Prompt 喂给 LLM（如接入智谱 GLM-4 或 OpenAI GPT-4 API），要求其以“苏格拉底式”启发提问的方式给出提示，防止学生直接抄袭答案。
2.  **知识图谱抽取**：利用 AI 自动分析题目的文本描述，抽取核心考察知识点（如“动态规划”、“背包问题”），用于自动丰富题库标签和精准推荐。

#### 5.2 竞赛晋级路径可视化
基于历年蓝桥杯、天梯赛等大型赛事的真实获奖分数线数据建立分析模型。当学生在系统中完成一套模拟卷后，系统会将成绩映射到历年数据中，利用 ECharts 绘制出该学生的“晋级概率曲线图”和“能力雷达图”，直观展示其距离“省一”、“国三”的差距及需要补强的专项模块。

---

### 六、 界面设计说明

*(实际文档中，此部分需大量插入系统截图，并配合文字说明。建议按照以下顺序展示)*

1.  **首页看板**：展示深色/浅色模式下的现代感 UI 设计，突出核心数据指标（打卡天数、排名）的可视化。
2.  **在线工作台（核心页面）**：
    *   左侧为题目描述与用例展示。
    *   右上为集成 Monaco Editor 的代码编辑区，支持语法高亮与代码提示。
    *   右下为控制台输出与 AI 助教对话框，展示 AI 如何对错误代码片段进行划线高亮及悬浮提示。
3.  **比赛大厅与合规中心**：展示比赛列表卡片设计，以及参赛前严肃的合规协议弹窗，强调系统的规范性。
4.  **错题本与晋级分析**：展示错题列表的筛选交互，以及由 ECharts 渲染的精美数据分析图表（柱状图、雷达图）。

---
**设计总结**
“数智启航”系统通过整合前沿的 Web 技术栈与大语言模型能力，重塑了程序设计竞赛的备考流程。系统不仅在技术架构上保证了高可用与安全性，更在应用逻辑上深刻切中了当前编程教育的痛点，具有极高的实践价值与推广前景。